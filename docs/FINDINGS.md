# 현재 결론과 각 주장의 상태

작성: 2026-09-23. 이 문서가 레포의 **현재 판단 기준**이다. `RESEARCH_DESIGN.md`는 같은 날의 작업 기록이며, 그 앞부분의 계획 중 상당수는 아래 결과로 수정되거나 폐기되었다.

상태 표기:
- **확인**: 정확한 계산 또는 짧은 증명으로 확인
- **수치**: 수치 탐색으로만 확인, 증명이 없다
- **선행**: 기존 문헌에 이미 있다
- **반증**: 검토 결과 성립하지 않는다
- **미정**: 판단에 필요한 작업이 남았다

---

## 1. 한 문단 요약

비표본 지역의 실제 유한모집단 평균에 대한 예측구간을 다룬다. 지금까지 확인한 것은 다음과 같다.
- (a) 조사 관측 깊이가 제한되면 분포 모양을 식별할 수 없다는 정확한 반례가 있다.
- (b) 잡음 섞인 보정 점수에서 잠재 목표로 넘어가는 log-concave 역합성곱 축소(LDC)를 수치로 계산했다.
- (c) split conformal의 Beta 법칙을 이용해 주변 보장을 정확히 계산했다.
- (d) apipop 유한모집단에서 LDC, noisy conformal, Fay–Herriot를 비교했다.

**표준 FH는 apipop 네 조건에서 명목 포함률을 지키고 LDC보다 12–17% 짧았다.** 그래서 “LDC가 FH의 더 나은 대체재”라는 주장은 지금 근거로 성립하지 않는다. 정규 구간의 모양 강건성 수치(87.75%)는 **대칭 log-concave에 대해 이미 알려진 결과(Barthe–Koldobsky)**다. 남은 새로움 후보는 잡음 섞인 보정자료에서 잠재 목표의 포함확률로 넘어가는 부등식과 그 유한표본 보장, 그리고 “어떤 조사 정보가 있어야 어떤 신뢰성이 가능한가”라는 질문이다. 논문을 두 편으로 나누는 결정은 **보류**한다.

**조건부 신뢰성 비교(E12–E13) 이후의 판단.** 같은 목표 Pr_D[Pr(θ_new ∈ C | D) ≥ .90] ≥ .95를 부과하면 다음과 같다.
- **FH_PAC:** 모수 bootstrap 허용계수를 쓴 FH로, 가장 짧다. 다만 비정규 모양과 apipop 두 조건에서 목표에 못 미친다.
- **LDC_PAC:** 모든 설정에서 목표를 지키지만 FH_PAC보다 11–20% 넓다.
- **오라클로 폭을 맞춘 비교:** 중심·척도를 어떻게 추정하든 폭이 거의 같다. 따라서 차이는 구간 모양이 아니라 **보장을 얻는 방식의 여유**(최악 경우 축소와 Beta 여유, LDC_PAC에서 약 15%)에서 나온다.

외부 검토(C27)로 세 가지가 더 드러났다.
- 작은 잡음에서 r = 1로 넘기는 처리는 비대칭 log-concave에서 안전하지 않다.
- E05 하한은 min/mean 순서를 고치면 LDC k = 102에서 .918 → .905다.
- 이질 D에서는 공통분산 D 하한이 명목 신뢰수준을 지키지 못한다.

그래서 **LDC_PAC는 아직 “보장된 절차”가 아니다.** 공통 D·log-concave 모형 안의 보장 논리 골격과 수치 후보가 있을 뿐이다.

**2026-09-24 이론 진전(C29–C34).**
- R_{p,q}를 닫힌 식과 2차원 최대로 정확히 계산하게 됐고, 기존 DE 표와 10⁻¹³ 수준으로 일치했다.
- 작은 잡음에서 비대칭 log-concave이면 모든 수준에서 √x 차수의 확대가 필요하다. 상수 c_q를 찾았고, 하한은 증명, 상한은 증명 골격이다.
- 축소·유지·확대의 경계 x*(q)를 그렸다.
- 이질 잡음에서는 평균 커널이 정확한 제약이다. 평균 분산 대입은 양방향으로 틀린다.
- 순서통계량 보정은 Hoeffding 비교로 이질 잡음에서도 유효하다. 이를 쓴 HetLDC는 합성 실험의 모든 모양에서 목표를 지켰다.

연구 순서는 두 단계다. 먼저 **공통·알려진 잡음에서 “잡음 관측의 포함확률 → 잠재 포함확률”의 최선의 보정(R_{p,q})을 증명하고 인증한다.** 이것이 Biometrika Miscellanea 후보다. 그 뒤 지역마다 다르고 추정도 불확실한 조사오차로 확장한다(JSSAM 후보, 분리 여부는 그때 판단). 모양 가정 없는 안전 기준 대비 log-concavity가 폭을 약 1.8–3.1배 줄인다(C28). 이것이 이 정리가 중요한 이유의 첫 근거다.

---

## 2. 주장별 상태

| # | 주장 | 상태 | 근거 | 주의 |
|---|---|---|---|---|
| C1 | 두 세계 반례: 지역당 PSU ≤ 3이면 관측 법칙이 같다(1–3차 factorial moment 일치, m = 4에서 TV 96/4753). | **확인** | `e01`, `test_core.py` | 두 점 논법 자체는 고전적이다 |
| C2 | 반례에서 90% 연결 구간의 최소 기대 폭: A만 유효하면 12.8%p, 두 세계 모두 유효하면 14.0067%p(LP). B를 알면 1.6%p. | **확인** | `e01` | 연결 구간 한정 |
| C3 | 연속형 확장: 평균 R² ≈ 0.893인 정확한 중심이 있어도 기대 폭 ≥ 0.0608(B를 알면 0.012). | **확인**(A만 유효 조건의 하한) | `e01` | 두 세계 제약을 모두 걸면 더 큰 sharp 값이 있다 |
| C4 | 관측 연산자 A_(m,r)의 랭크 = C(m+r, r). 두 설계를 쌓으면 C(m+r,r)+C(m'+r',r')−C(min m+min r, min r). | **확인**(증명 스케치 + 11개 조합 계산) | `e01`, `START_GATE_MEMO.md` 3.2절 | 선행 표현 여부 미확인 |
| C5 | 설계를 섞으면 식별되는 모멘트 차수가 늘어난다(2×4 + 4×2: q = 2 → 4). | **확인** | `e01` | |
| C6 | 그러나 무한 표본 극한에서 설계 혼합의 **식별 폭**은 더 나은 단일 설계와 같았다(분포 3개, 조합 4개). | **수치** | `e02`, `results/design_mixing_identified_width.csv` | 작은 모형(θ 간격 1/24) |
| C7 | 학습·보정 지역의 조사 깊이를 차등화해도 균일 설계 대비 폭 이득 0.6–2.3%. | **수치**(대략 계획 모형) | `e10` | 모형이 거칠다 |
| C8 | log-concave에서 (1−α) 분위수의 |X−EX|/sd 상한 1.7559(90%), 2.1691(95%). 극값은 절단 Laplace. | **선행**(대칭 경우) + **수치**(비대칭) | `e03`; Barthe–Koldobsky, He–Tkocz–Wyczesany(arXiv:2305.06235) Lemma 3 | 대칭 log-concave에서는 기존 결과다. 비대칭은 1–3조각 전역 탐색에서 대칭 극값을 넘지 못했을 뿐이며 증명이 아니다 |
| C9 | 참 평균·분산을 쓰는 μ ± z·sd 구간의 최악 포함률: 90% → 87.75%, 95% → 92.97%. | **선행**(대칭) | `e03`; 위 문헌 | 절단 대칭 Laplace 족에서만 계산했다. “log-concave이면 모두 87.75% 이상”이라고 쓰려면 비대칭 근거가 필요하다. 이것은 **참 중심·분산**을 쓰는 구간에 대한 결과이며, 추정된 FH 구간의 실패 원인 분석이 아니다 |
| C10 | **LDC 부등식**: W log-concave, e ~ N(0, D) 독립, P(\|W+e\| ≤ t) ≥ 1−α ⇒ P(\|W\| ≤ t·r_α(D/t²)) ≥ 1−α. 극값은 한 구간 위 log-affine(절단 지수). | **수치** | `e04`, `results/r_table.json`, `logconcave.py` | 극값 축소는 명제 1로 증명했다(Fradelizi–Guédon localization + 수준 손실 없는 절단, 2026-09-24). DE 탐색은 sup을 과소평가할 수 있고(비보수적 방향), 인증 상한은 C39에 있다. 인증된 표는 C39에서 만들었다(명제 1과 부동소수점 여유에 의존). 선행연구 대비 미완. **신규성 미정**. 문헌 대조(C26)와 격자 교차검증(C25) 참고 |
| C11 | r_α(x)는 x ≥ .0023(α = .10)에서 x에 대해 단조감소하고, 그보다 작은 x에서는 증가한다(r > 1, 최대 1.00063). | **수치** | `e04`, `e16`, `e19`, `test_core.py` | 조건부 보수적 조회의 근거. `ShrinkTable`은 최대점 왼쪽에서 전체 최댓값을 돌려준다 |
| C12 | split conformal의 k번째 순서통계량 t에서 P(\|V\| ≤ t) ~ Beta(k, K+1−k). 이것과 H(p; x) 표로 log-concave 잠재 포함률의 **수치 하한**을 적분한다. x = D/T²가 T와 함께 움직이므로 하한은 E_U[min_x H]이다(2026-09-23 수정; 이전 코드는 min_x E_U[H]를 계산했다). | **확인**(Beta 법칙) + **수치**(H) | `e05`, `results/exact_coverage.csv` | 잡음 분산 공통, 가우스 잡음, W와 독립 가정. H는 벌점을 둔 DE 탐색값이라 infimum의 **상한 추정**이다. 인증된 하한이 아니다. x는 네 점뿐이다 |
| C13 | 현재 하한 논법의 값(K = 110). noisy CP: k = 100에서 .867, k = 102에서 .916. LDC k = 102는 단계별로 .9177(이전 코드, min_x E[H]) → .9141(순서 수정, E[min_x H]) → .9049(D 하한 실패확률 η = .01 반영, 독립을 가정한 곱 .99). LDC k = 105는 .9368. | **수치** | `e05` | **실제 최악 포함률이나 필요조건이 아니다.** 하한을 달성하는 분포나 최적성 증명이 없다. “k = 102가 필요하다”가 아니라 “이 논법에서 90%를 넘는 첫 후보 순위”다. .99를 곱하려면 두 사건이 독립이어야 하며, 그렇지 않으면 η를 빼야 한다(.9041) |
| C14 | 합성 MC(D/A = 0.577, K = 110): LDC는 log-concave 모든 경우에서 90% 이상이고 noisy CP보다 짧다. Gaussian plug-in은 절단 Laplace에서 88.1%. | **수치** | `e06`, `results/synthetic_mc.csv` | |
| C15 | apipop, 같은 보장(k = 102)에서 LDC는 noisy CP보다 4.2–7.8% 짧다(레포 재실행; 이전 실행 4.4–7.7%). | **수치**(1,000회) | `e07`, `results/apipop_ldc_k102_summary.csv` | 지역별 D_c가 이질적이라 이론 가정 밖이다 |
| C16 | apipop에서 **FH(REML, bootstrap)는 명목 포함률을 지키고 LDC보다 12–17% 짧다.** | **수치**(4조건 × 1,000회) | `e08`, `results/apipop_fh_summary.csv` | 한 자료, 네 조건에 대한 결과이며 “FH는 안전하다”의 증명이 아니다. FH bootstrap 이론도 가정 아래의 근사다 |
| C17 | “FH는 반복의 10–17%에서 관측 포함률이 85% 미만”(레포 재실행; LDC 1–2%) | **해소**(C21–C24) | `e08`, `e12`, `e13` | 표준 FH는 **주변** 90%를 목표로 하므로, 조건부 포함률이 90% 아래인 반복이 절반 가까이 되는 것은 설계대로다. 평가 잡음을 없앤 E12와 같은 목표를 부과한 E13에서 다시 비교했다 |
| C18 | \|V\| 분위수 여러 개를 제약으로 써도 축소 상한 개선은 작다(정확히 알 때 0.874 → 0.859 → 0.849). | **수치** | `e09`, `results/multi_quantile_ceiling.json` | “K ≈ 110에서의 정보 한계”라는 일반 주장은 하한 정리가 없으면 할 수 없다. 지금은 이 방법에서 개선이 작았다는 말까지만 가능 |
| C19 | Gaussian D/A 모형이 후속 메모의 apipop 수치를 재현한다(noisy 폭 63.28 대 63.35 등). | **수치**(사후 계산 1조건) | `RESEARCH_DESIGN.md` 2절 | 사전 등록된 검증이 아니다 |
| C20 | PUMS(RI): 25–64세 성인 가구의 92%가 1–2명, PUMA 간 취업률 sd 0.021. | **확인** | `e11`, `results/pums_structure_ri.json` | 한 주 |
| C21 | 평가 잡음 없는 합성 실험(G를 알아 조건부 포함률을 정확히 계산, 7개 모양 × D 공통/이질, 2,000회): 표준 FH(정규, bootstrap)는 평균 포함률 .88–.93이지만 Pr_D[조건부 포함률 ≥ .90]는 .32–.89다(절단 Laplace .32–.39, 정규 .58–.60). | **수치** | `e12`, `results/conditional_synth_*.csv` | FH가 틀린 것이 아니라 목표가 다르다(주변 보장) |
| C22 | PAC 규칙(목표 Pr_D[cov ≥ .90] ≥ .95): CP_PAC(k = 105)와 LDC_PAC는 14개 설정 모두 ≥ .986(비-log-concave t3 포함). FH_PAC(모수 bootstrap 허용계수)는 .917–.998로 **Laplace·절단 Laplace + 이질 D에서 목표 미달**(.940, .917). 폭: FH_PAC 3.89–4.12 < LDC_PAC 4.34–4.83 < CP_PAC 4.70–5.12. LDC_PAC는 FH_PAC보다 11–20% 넓고 CP_PAC보다 4–8% 짧다. | **수치** | `e12`, `e15`(해석적 CDF 재계산) | LDC_PAC의 보장 논리는 log-concave·공통 D 가정 아래 Beta 법칙 + LDC 부등식(C10)이며, 표의 인증은 C39에서 추가했다(명제 1에 의존). **이질 D에서는 D 하한 자체가 명목 99%를 지키지 못한다**(C27). 이질 D의 결과는 경험적 성능일 뿐이다 |
| C23 | 같은 신뢰성으로 맞춘 폭(규칙마다 폭 배율 하나를 오라클로 조정, 진단용): Pr_D ≥ .95 기준에서 noisy CP가 가장 짧거나 동률이고, FH는 0–11%(비대칭 모양에서 큼), LDC는 1–8% 넓다. 평균 포함률 .90 기준에서는 모든 규칙이 서로 3% 이내다. LDC_PAC의 실제 폭은 자기 오라클 폭보다 약 15%, FH_PAC는 약 6% 넓다. | **수치** | `e12`, `results/conditional_synth_matched.csv` | **중심·척도 추정 방식에서는 폭 이득이 거의 없다.** 격차는 보장 방식(최악 경우 축소 + Beta 여유)에서 나온다 |
| C24 | apipop 같은 비교(4조건 × 1,000회, 평가 지역 105개 = 비표본 지역 전체라 관측 포함률이 곧 유한모집단 조건부 포함률): FH_normal Pr .55–.88. FH_PAC .905–.993(api99_n3 .932, socio_n3 .905로 목표 미달). LDC_PAC .985–.995, CP_PAC .997–1.0. 폭(api99_n3): FH_PAC 59.5, LDC_PAC 70.4(+18%), CP_PAC 74.2. 오라클로 맞추면 60.5–64.2로 모두 비슷하고 noisy CP가 가장 짧다. | **수치** | `e13`, `results/conditional_apipop_*.csv` | 남은 105개 지역 전체가 목표 집합이므로 이 비율이 곧 조건부 포함확률이며, 이항 평가 잡음을 빼면 안 된다(이전 버전의 해당 열은 삭제). 다만 고정된 지역 목록에서 비복원으로 나누는 구조라 iid 지역을 가정한 Beta 보장이 자동으로 적용되지는 않는다 |
| C25 | DE 표(E04)의 격자 교차검증: β × 위치 × 척도 3차원 격자 + Dirac, α ∈ {.10, .07}, x 15점씩(30점). 격자가 표를 넘은 점은 없다(차이 ≤ 0). x ≥ .026에서는 차이가 0.0035 이내다. x = .001의 표 값 1.027은 구적 오차다. 그 DE 점(β = −77, 척도 64)은 적응 적분으로 noisy 질량 .8966 < .90이라 제약을 만족하지 않는다. **그러나 r > 1 자체는 실제다.** W ∝ e^w on [0, 1.0681], x = .0005에서 noisy 질량 .900041 ≥ .90이지만 잠재 질량은 .899697 < .90이다(해석식·적응 적분 일치, 외부 검토 제시, `test_core.py`). 격자도 x = .001에서 1.0003을 찾았다. | **수치** + **확인**(반례) | `e14`, `results/r_grid_check.csv`, `test_core.py` | 격자와 DE는 같은 구적법을 쓰므로 독립 검증이 아니다. sup의 유효한 후보는 r의 **하한**만 준다. 안전한 구간에는 r의 인증된 **상한**이 필요하다. 이전 코드는 x < .001에서 r = 1을 돌려줬는데, 이 반례 때문에 보수적이지 않아 표의 첫 값(1.027)을 돌려주도록 고쳤다. 다만 1.027은 구적 오차가 섞인 계산값이다. sup_{0<x<.001} r(x) ≤ 1.027은 **증명되지 않은 임시값**이다 |
| C26 | 문헌 대조: 대칭 unimodal W에서는 **r = 1(축소 없음)이 Anderson(1955) 정리로 바로 성립**하며, noisy conformal에 대해 Einbinder et al.(JMLR 2024, Prop. 1)이 같은 내용을 정리했다. 따라서 새로움 후보는 (i) 비대칭 포함 log-concave 전체에서 sharp한 r_α(x), (ii) 극값 특성화, (iii) 비표본 지역 잠재 목표의 유한표본 보장이다. 비대칭 log-concave에서는 작은 잡음에서도 r > 1이 필요할 수 있다(C25 반례). 대칭 결과는 비대칭으로 그대로 옮겨지지 않는다. | **선행**(대칭 r = 1) + **미정**(나머지) | 문헌 조사 | 가장 가까운 연구: Cohen–Goldberger–Tirer(arXiv:2509.15120; 알려진 가우스 잡음 분산으로 점수 역합성곱, 보장 정리 없음), Zhang–Tuoto(arXiv:2608.02766; FH 틀의 conformal, 표본 지역만, 축소 없음). 외부 검토가 두 편의 방법 절을 확인했다. Cohen 등의 v1에는 유한표본 PAC 인증 정리가 없지만, “잡음을 역합성곱해 구간을 줄인다”는 발상은 이미 있다. Fradelizi–Guédon은 컴팩트 지지집합을 요구하므로 [−M, M] 절단 후 극한 논증과 Dirac 처리를 증명에 적어야 한다(1차원에서 조건 자체는 충족: 제약함수 연속, 목적함수 선형·usc). He–Tkocz–Wyczesany Lemma 3이 대칭 log-concave에서 절단 대칭 지수로의 축소(Barthe–Koldobsky)를 담고 있음을 원문으로 재확인했다(C8, C9 근거 유지) |
| C27 | 외부 검토(2026-09-23/24)의 지적을 재계산으로 확인했다. (a) C25의 비대칭 반례. (b) E05의 min/mean 순서(C12, C13). (c) 이질 D(E12 설정, 10만 회)에서 공통분산 chi-square 하한 D_low가 표본 지역들의 실제 평균 D 이하인 비율은 .9667, 생성분포 평균 .577 이하인 비율은 .8626이다(공통 D에서는 .990). (d) `shrink_factor`는 시작 척도에서 noisy 질량이 부족한 위치를 통째로 제외한다. 그런데 척도를 키우면 질량이 늘었다 줄 수 있어서(예: x = .136, Unif[−a, a], a ≈ .8536) 검색 영역이 완전하다는 증명이 없다. | **확인**(a–c) + **지적 수용**(d) | `test_core.py`, `e05` | (c)는 구간 포함률이 아니라 분산 하한의 적중률이다. 평균 D를 정확히 알아도, 서로 다른 정규 잡음의 혼합은 공통 정규 잡음이 아니다 |
| C28 | 모양 가정 없는 안전 기준(외부 검토 제안): P(\|V\| ≤ t) ≥ p > q, D ≤ U이면 s = t + √U · z_{1−(p−q)/2}에서 P(\|W\| ≤ s) ≥ q다(\|W\| ≤ \|V\| + \|e\|와 합집합 상계). k = 105, K = 110의 p_k = .9036, q = .90, t = 1 기준으로 x = .061/.101/.136/.201에서 s = 1.72/1.93/2.07/2.31이다. 같은 x의 LDC r(.9, .9)은 .96/.92/.87/.75다. | **확인**(짧은 증명) + **수치** | `procedures.union_bound_halfwidth`, `test_core.py` | 신규 기여가 아니라 기준선이다. **log-concavity가 폭을 약 1.8–3.1배 줄여 준다**는 것이 LDC 정리의 가치를 재는 척도다. LDC 값은 p = q = .90 표라 p_k = .9036을 쓰면 조금 더 작다 |
| C29 | **정확한 2차원 축소.** R_{p,q}(x)의 극값족(Dirac, 한 구간 위 log-affine)에서 noisy 질량은 Φ만으로 된 닫힌 식이다(적응 적분과 9×10⁻¹² 이내). 이동에 대해 noisy 질량과 잠재 질량이 log-concave라(Prékopa) 가능한 이동의 두 끝점만 보면 되므로, R은 (기울기, 길이) 2차원 최대로 계산된다. 이 계산(E16)과 독립적인 DE 표(E04)는 x ≥ .006의 73개 표 점에서 1.5×10⁻¹³ 이내로 일치한다. | **증명**(닫힌 식, 끝점 축소, localization 절단 극한) + **수치** | `src/uai/extremal.py`, `e16`, `docs/THEORY_NOTE.md` 1절 | 격자 최대는 R의 하한으로 수렴한다. 인증된 상한은 아직 없다 |
| C30 | **작은 잡음 법칙.** 모든 q와 **모든 x > 0**에서 R_{q,q}(x) ≤ 1 + c_q√x이고, x → 0에서 R_{q,q}(x) = 1 + c_q√x + o(√x)다(c_q > 0; c_.80 = .0461, c_.90 = .0191, c_.95 = .0084, c_.99 = .0014). c_q는 한쪽 문제의 값이며, 그 극값은 지수꼬리다(보조정리 A). 비대칭 log-concave에서는 잡음이 작을 때 noisy 보정이 **모든 수준에서** 잠재 목표를 √x 차수로 놓칠 수 있지만, 놓치는 양은 모든 x에서 c_q√x 이하다. 대칭이면 이 현상이 없다(Anderson). | 상한(비점근)·하한·보조정리 A·B **증명**, c_q 값은 **공 산술 인증**(C42) | `docs/THEORY_NOTE.md` 2절, `e16` | 상한 증명은 양쪽 실패 예산을 두 한쪽 문제로 나누는 논법이며, 명제 1(localization)에 의존하지 않는다. 격자 R은 x = 10⁻⁶–10⁻²에서 모두 상한 이하다. c_q 값은 E27에서 공 산술(Arb)로 인증했다(C42) |
| C31 | **축소·유지·확대 경계.** R_{q,q}(x) = 1인 x*(q) = .034/.017/.0062/.0012(q = .80/.85/.90/.95)다. 그 아래에서는 넓혀야 하며, 최대 확대는 0.37/0.17/0.063/0.012%다. | **수치** | `e19`, `results/boundary_map.csv`, `results/fig_boundary_map.png` | |
| C32 | **이질 잡음에서 평균 분산 대입의 오류는 양방향이다.** 정확한 평균 커널의 R과 비교하면 대부분 대입이 보수적(최대 .008)이지만, x̄ = .2, 퍼짐 1.0에서는 대입이 1.4% 짧다. 최소 분산 대입은 최대 14% 넓다. | **수치** | `e18`, `results/hetero_kernel.csv` | 격자 12점 양자화로 계산했다. 모집단 극한에서 이 경우의 극값 분포(β = 1.108, 길이 1.256, 이동 −1.003)를 쓰면, 평균 분산 대입 반경(보수적 표 조회 .7624)의 잠재 포함률은 .8989 < .90이고 HetLDC 반경(.7648)은 .9000이다. **이론적으로는 무효지만 실제 포함률 손실은 0.1%p 정도로 작다.** |
| C33 | **순서통계량의 Beta 하한은 이질·독립 잡음에서도 유효하다.** 평균 CDF F̄에 대해 {F̄(|V|_(k)) < p}는 Poisson-binomial 꼬리 사건이고, Hoeffding(1956)에 따라 k ≥ Kp + 1이면 Bin(K, p) 꼬리 이하다. 이 부등식은 Sen(1970, Ann. Math. Statist. 41)의 이질 순서통계량 비교의 한 경우라 **새 결과가 아니다**. 새로운 것은 HetLDC에 쓴 것뿐이며, 원고는 Sen을 인용하도록 고쳤다. | **증명**(선행: Sen 1970) | `docs/THEORY_NOTE.md` 명제 7, `procedures.hetldc_halfwidth` | D_i가 알려져 있고 잡음이 W와 독립인 가우스일 때 |
| C34 | **HetLDC 합성 평가**(E20; 이질 D 알려짐, 4개 모양 × 150회). 목표 Pr_D[cov ≥ .90] ≥ .95 대비 신뢰성: HetLDC .967–1.0(모든 모양 충족), LDC_PAC(평균 분산) .980–1.0, CP_PAC 1.0, FH_PAC .847–1.0(절단 Laplace .847, Laplace .927로 미달). 폭: HetLDC는 LDC_PAC보다 3.4–3.5%, CP_PAC보다 9.7–11% 짧고 FH_PAC보다 12–18% 넓다. | **수치** | `e20`, `results/hetldc_synth_summary.csv` | 150회라 신뢰성의 Monte Carlo 표준오차는 약 .018이다 |
| C35 | **HetLDC apipop 적용**(E21; 4조건 × 120회, 추정 D̂_c를 알려진 값처럼 대입, 남은 105개 교육구의 포함 비율). 신뢰성: HetLDC .942–.992(api99_n3 .942는 목표 .95보다 약간 낮으나 MC 오차 ±.02 안), LDC_PAC .983–1.0, CP_PAC 1.0, FH_PAC .908–.992(api99_n3 .908, socio_n3 .925로 미달). 폭: HetLDC는 LDC_PAC보다 0.8–5.2%, CP_PAC보다 7.7–10.4% 짧고, FH_PAC보다 10–16% 넓다. | **수치** | `e21`, `results/hetldc_apipop_summary.csv` | 교육구당 학교 2–3개라 D̂_c의 자유도가 1–2다. 어느 규칙의 보장도 이 경우를 직접 덮지 않는다 |
| C36 | **예산 배분과 인증 반폭**(E23, 모집단 극한, 평균 학교 수 3 고정, p = .9036, q = .90). 정규 잠재분포에서 다섯 가지 배분(균등부터 1/5 극단 분할까지)의 인증 반폭 T·R^mix는 1.786–1.799로 0.7% 이내다. 잡음이 커지면 T가 커지는 만큼 정확한 R이 줄기 때문이다. Laplace와 치우친 gamma에서는 균등 배분이 가장 좋고 극단 분할은 3.8–3.9% 넓다. 잡음이 없으면 반폭 1.662, D = .577이면 1.789(+7.6%)이고 축소 없는 noisy CP는 2.088(+26%)이다. **정확한 전달이 잡음으로 늘어난 폭의 약 70%를 되찾는다.** 오라클(참 분포의 분위수) 대비로는 정규 +8.8%, Laplace +7.4%, 치우친 gamma +24.5%다. | **수치** | `e23`, `results/allocation.csv` | K → ∞ 모집단 극한이다. 유한 K의 PAC 여유는 p = .9036에만 반영했다 |
| C37 | **추정 분산 대입의 견고성**(E22; E20의 이질 D, D̂_i = D_i χ²_ν/ν를 알려진 값처럼 대입, ν = 4·10, 2개 모양 × 120회). 신뢰성: HetLDC .975–.992, LDC_PAC .992–1.0, CP_PAC 1.0, FH_PAC .925–.992(절단 Laplace ν = 10에서 .925로 미달, ν = 4는 .950). 폭: HetLDC는 LDC_PAC보다 4.6–4.9%, CP_PAC보다 10.0–10.6% 짧고 FH_PAC보다 5.9–10.1% 넓다. 알려진 D(C34)보다 HetLDC–FH 격차가 줄었다. | **수치** | `e22`, `results/estimated_variance_summary.csv` | 어느 규칙의 보장도 이 경우를 덮지 않는다. D̂와 V가 독립이라는 가정(정규 단위자료)이다. 120회라 신뢰성의 MC 표준오차는 약 .02다. 추정 분산의 바깥 신뢰집합 처리는 아직 없다 |
| C38 | **PAC 여유는 모든 x에서 확대를 없앤다.** R_{p,q}(x) ≤ 1 + C_{p,q}√x(모든 x; C_{p,q}는 양쪽 예산 배분의 sup-inf). 수치로는 C_{p,q} = c_{p,q}(한쪽 몰기가 최악)였다. C_{.9036,.90} = −.0572이므로, K = 110·k = 105의 LDC_PAC는 확대 없이 적어도 1 − .057√x만큼 축소된다. C ≤ 0이 되는 최소 여유는 q = .80/.90/.95에서 4.4×10⁻³/8.0×10⁻⁴/1.6×10⁻⁴다. | 부등식 **증명**, 상수는 **공 산술 인증**(C42; −.0572 ≤ C_{.9036,.9} ≤ −.057, 최소 여유 구간) | `docs/THEORY_NOTE.md` 정리 5+, 따름정리 6 | 이전의 따름정리 6은 점근 수치 주장이었다. 격자 R_{.9036,.9}는 x = 10⁻⁵–.061에서 모두 상한 이하다 |
| C39 | **R_{p,.9}의 인증된 상한 표**(E24; p = .90, .9036, x = .010–.366, 713점씩). 방법: 극값족 매개변수(β, ℓ, b)에서 W가 확률적으로 단조라는 사실을 이용한 상자 경계와 가지치기(명제 10), 그리고 잡음 수준 연속성 R(x + h) ≤ R(x) + c_q√h(명제 9, **증명**)로 격자 사이를 메운다. 인증 성공률 99.9%/99.0%(가능성 경계 x ≈ .36–.37 직전만 실패). x ∈ [.05, .3]에서 U − L ≤ 3×10⁻⁴·L이다. 조회값은 x ∈ [.01, .34]에서 L의 최대 1.012배다. 이전 표보다 x ∈ [.02, .3]에서 최대 0.15% 크고 중앙값은 0.996배다. 격자의 가능한 값이 이전 표를 넘은 곳은 없다. | 방법·명제 9 **증명**, 값은 **부동소수점 인증**(여유 10⁻⁹, 닫힌 식 오차 ≤ 1.9×10⁻¹² 대 mpmath) | `src/uai/certify.py`, `e24`, `results/certified_R.csv`, `procedures.CertifiedShrinkTable` | 명제 1(극값족 축소, 2026-09-24 증명 완료)에 의존한다. 구간산술이 아니다. x < .01은 정리 5/5+의 닫힌 상한이라 0.3%까지 느슨하다 |
| C40 | **인증 값을 쓴 LDC_PAC**(E25; E12 설정, 7개 모양 × 공통/이질 D × 2,000회). 인증된 R_{.9,.9}를 쓰면 이전 표보다 0.20–0.24% 짧다. 순서통계량 수준 p_k = .9036의 인증 표(R_{.9036,.9})를 쓰면 1.6–1.7% 짧다. 신뢰성 Pr[cov ≥ .90]은 최저 .985(절단 Laplace 이질 D)로 목표 .95를 넘는다. | **수치** | `e25`, `results/certified_ldc_summary.csv` | p_k 표 사용은 보장 논리 안에서 공짜다(Beta 사건 위에서 noisy 질량 ≥ p_k). 이질 D와 t3는 여전히 가정 밖이다 |
| C41 | **HetLDC의 자료별 인증**(E20 재실행, E26; 이질 D 알려짐, 4개 모양 × 150회). 혼합 커널 R^mix_{p_k−ε,.9}를 가지치기로 인증했다. 600개 자료 모두 인증 성공(상대 여유 10⁻³: 368, 3×10⁻³: 222, 10⁻²: 10)이라 모양 가정 없는 대체값은 쓰이지 않았다. 인증 비용은 격자값 대비 폭 0.2%다. 신뢰성: HetLDC_cert .967–1.0, LDC_cert_pk(평균 분산, R_{.9036,.9} 인증 표) .973–1.0, 모두 목표 충족. 폭: HetLDC_cert는 LDC_cert_pk보다 1.6%, 기존 LDC_PAC보다 3.2–3.3%, CP_PAC보다 9.5–10.5% 짧고 FH_PAC보다 12.5–18% 넓다. | **수치**(값은 부동소수점 인증) | `e20`, `procedures.hetldc_certified`, `results/hetldc_synth_summary.csv`, `paper/main.tex` 표 1 | 명제 1과 명제 7에 의존한다. 150회라 신뢰성의 MC 표준오차는 약 .018이다 |
| C42 | **상수의 공 산술 인증**(E27, `src/uai/interval.py`, Arb). 판정 부등식은 모두 공 산술로 계산하고, 부동소수점은 후보점 제안에만 썼다. b_u(p)가 u에 대해 비증가라는 단조성으로 상자 경계를 세우고, u → 0과 u → ∞ 끝은 Mills 비와 극한값으로 닫았다. 결과: c_.80 ∈ [.04607304, .04607306], c_.85 ∈ [.0315871, .0315872], c_.90 ∈ [.01906182, .01906187], c_.95 ∈ [.00840751, .00840763], c_.99 ∈ [.0013950, .0013958]. C_{p,q} ≤ γ는 한쪽 잠재 예산 φ_γ(a)의 합 조건과 동치이며, 적응 분할로 C_{.9036,.9} ≤ −.057(하한 c_{.9036,.9} ≥ −.0571845)을 인증했다. 최소 여유: C_{q+δ,q} ≤ 0을 (q, δ) = (.8, .005), (.9, .001), (.95, .0002)에서 인증했고, c_{q+δ,q} > 0을 δ = .0043, .00079, .00015에서 인증했다. 따라서 최소 여유는 (4.3, 5.0]×10⁻³, (7.9, 10]×10⁻⁴, (1.5, 2.0]×10⁻⁴ 구간에 있다. | **인증**(공 산술) | `e27`, `results/interval_constants.json`, `test_core.py` | 보조정리 A와 정리 5+의 동치 변형에 의존한다. `CertifiedShrinkTable.C_SPLIT[.9036]`을 인증되지 않은 −.05718에서 −.057로 고쳤다(x < .01에서만 쓰이며, E20·E25 결과에는 영향이 없다). R 표 자체(C39)는 여전히 부동소수점 인증이다 |
| C43 | **신규성 재조사**(2026-09-24, 문헌 검색). 기여 1–4(R_{p,q} 정의, 극값족 2차원 축소, 작은 잡음 법칙 1 + c_q√x, 여유와 인증 축소)를 증명한 선행연구는 찾지 못했다. 가장 가까운 연구와의 차이: Einbinder et al. 2024(대칭만), Cohen–Goldberger–Tirer 2025(보장 없는 역합성곱 축소; 축소 **발상**은 선행), Guille-Escuret–Ndiaye 2024(arXiv:2405.18601; 잡음 중앙값이 0이면 모양 가정 없이 1 − 2α를 보장하고, 비-가우스 잡음 예에서 잠재 포함률이 noisy 포함률보다 낮다; 원문 명제 2.3과 부록 D.1 확인), Armstrong–Kolesár–Plagborg-Møller 2022(적률 제약 아래 최악 경우 EB 구간, 표본 단위). 기여 5는 Sen(1970)과 Hoeffding(1956)의 선행 결과다(C33). 원고의 “대칭이 없으면 알려진 것이 없다” 문장은 Guille-Escuret–Ndiaye를 인용해 고쳤고, 위 문헌과 Kannan–Lovász–Simonovits 1995, Chatterjee–Lahiri–Li 2008, Yoshimori–Lahiri 2014, Bersson–Hoff 2024, Zhang–Tuoto 2026을 추가했다. | **문헌 조사** | `paper/main.tex` 서론·참고문헌 | 검색으로 선행연구가 없다는 것을 증명할 수는 없다. 판정: 1·3·4는 새로울 가능성이 높고, 2는 도구는 기존이며 적용이 새롭고, 5는 일부 알려져 있다. arXiv:2305.06235(He–Tkocz–Wyczesany, “A sharp Gaussian tail bound for sums of uniforms”, Israel J. Math. 269, 2025)는 원문 PDF로 다시 확인했다. Lemma 3(Barthe–Koldobsky)이 대칭 log-concave에서 절단 대칭 지수로의 축소를 담고 있어 C8·C9의 인용은 유효하다 |

### 반증되었거나 철회한 주장
- “설계 혼합이 비표본 지역 구간을 짧게 만든다” → C6에서 반증(작은 모형 한정).
- “log-concave 인증으로 noisy CP보다 10% 이상 짧다” → apipop에서 4–8%(C15).
- “기존 방법(FH)은 실제 평균에 대한 보장이 없어 실패한다” → apipop에서 반증(C16).
- “87.75%와 절단 지수 극값은 새로운 결과” → 선행 결과(C8, C9).
- “LDC 부등식은 높은 신규성이 있어 별도 이론 논문이 된다” → **판단 보류**(C10). 대칭 경우의 r = 1은 선행(C26).
- “작은 잡음에서는 축소 없이(r = 1) 넘겨도 안전하다” → 비대칭 log-concave 반례로 반증(C25).
- “noisy CP의 최악 잠재 포함률은 .867, 90%에는 k = 102가 필요하다” → 현재 하한 논법의 값과 후보 순위일 뿐이다(C13).
- “E12의 조건부 포함률은 정확하다” → 원래 계산은 2e6 참조표본의 경험 CDF였다. E15에서 해석적 CDF로 다시 계산했고(`src/uai/latent_laws.py`), 차이는 평균 포함률 ≤ .0001, 신뢰성 ≤ .0020, matched 폭 ≤ .0074로 결론은 바뀌지 않는다(FH_PAC 최저 .9165, LDC_PAC 최저 .9875). 이제 E12 수치의 기준은 `results/conditional_synth_exact_*.csv`다.
- “LDC가 noisy CP보다 짧은 것은 더 나은 구간 모양 때문” → 같은 신뢰성으로 맞추면 LDC가 오히려 1–8% 넓다(C23). LDC의 이득은 보장을 유지하면서 CP_PAC보다 짧다는 데 있다.
- “FH는 조건부로 신뢰할 수 없다” → 표준 FH는 주변 목표이므로 당연하다. 같은 조건부 목표를 부과한 FH_PAC는 대부분 목표를 지키고 가장 짧지만, 비정규 모양과 apipop 두 조건에서 미달했다(C22, C24).
- “FH가 실패한다면 원인은 모양이 아니라 분산 추정이다” → 참 중심·분산 구간에 대한 C9만으로는 말할 수 없다. 추정된 FH 구간의 실패 원인은 별도 분석이 필요하다.
- “model assistance가 D/A를 낮춰 위험을 없앤다” → 표본오차는 줄일 수 있다. 잔차의 지역 간 분산 A, 중심 추정오차, 분산 추정 불확실성도 함께 확인해야 한다.

---

## 3. 다음 작업 (2026-09-24 갱신, 1·2번 완료, 4번 진행 중)

1. ~~정리 5의 빈틈과 보조정리 A~~ → 완료(C30, C38). c_q·C_{p,q}의 공 산술 인증도 완료(C42). 명제 1의 절단 극한도 증명했다(수준 손실 없는 절단).
2. ~~R의 인증된 상한~~ → 완료(C39, C40). HetLDC(혼합 커널) 자료별 인증도 완료(C41). 남은 것: x ∈ [.002, .01]의 상자 인증, 구간산술.
3. 추정 분산의 바깥 신뢰집합 처리(E21·E22는 D̂를 알려진 값처럼 대입한 경험적 결과만 있다).
4. **짧은 원고**: LaTeX 초안 `paper/main.tex`(그림 1·2, 표 1 채움, 증명 절). 남은 것: TeX 엔진으로 컴파일 확인, 참고문헌 점검, 증명 절과 `MANUSCRIPT_PROOFS.md` 대조.
5. 그 뒤 PUMS와 조사설계 질문으로 돌아간다(JSSAM 후보).
