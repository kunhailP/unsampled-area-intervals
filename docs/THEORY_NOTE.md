# 잡음 관측에서 잠재 포함확률로: 이론 메모

작성: 2026-09-24. Biometrika Miscellanea 후보의 수학적 핵심을 정리한다. 상태 표기는 `FINDINGS.md`와 같다: **증명**(여기에 증명 또는 증명 스케치가 있음), **수치**(계산 근거만 있음), **추측**.

## 0. 문제

W는 잠재 잔차(비표본 지역의 참값 − 중심)이고 V = W + e, e ~ N(0, D)는 W와 독립이다. 임계값 t로 척도를 맞춰 t = 1, x = D/t²로 둔다. 관측으로 확인할 수 있는 것은 noisy 포함확률 P(|V| ≤ 1) ≥ p이고, 원하는 것은 잠재 포함확률 P(|W| ≤ s) ≥ q다. log-concave 분포 집합을 LC라 하면 최선의 보정은

  R_{p,q}(x) = sup { Q_q(|W|) : W ∈ LC, P(|W + √x Z| ≤ 1) ≥ p },  Z ~ N(0, 1)

이다. 반폭 t·R_{p,q}(D/t²)는 이 정보만으로 보장되는 가장 짧은 반폭이며, 더 짧으면 어떤 LC 분포에서 실패한다. “최선”은 이 가정과 이 제약 아래에서의 뜻이다. 보정자료 전체를 쓰는 모든 절차 중 최적이라는 뜻은 아니다.

기준선: Anderson(1955)에 따라 W가 대칭 단봉이면 P(|W+e| ≤ 1) ≤ P(|W| ≤ 1)이므로 대칭 부분족에서는 R_{q,q} ≤ 1이다(Einbinder et al. 2024, Prop. 1과 같은 내용). 모양 가정이 없으면 s = 1 + √x z_{1−(p−q)/2}가 안전하다(`FINDINGS.md` C28).

## 1. 극값의 축소 — **증명**(스케치; 절단 극한은 적어야 함)

**명제 1.** R_{p,q}(x)는 Dirac 질량과 “한 구간 [a, b] 위의 log-affine 밀도 ∝ e^{βw}”들에 대한 sup과 같다.

스케치. R ≤ s ⇔ inf{P(|W| < s) : W ∈ LC, ∫g dμ_W ≥ p} ≥ q, 여기서 g(w) = P(|w + e| ≤ 1)는 연속이다. 목적함수는 선형이고 열린 구간의 지시함수라 −P(|W| < s)는 약위상에서 상반연속이다. 지지집합을 [−M, M]으로 제한하면 Fradelizi–Guédon(2004)의 한 제약 localization이 적용되어 극점은 Dirac 또는 log-affine-on-segment다. 이 가족은 s와 무관하다. M → ∞: LC 분포를 [−M, M]으로 조건부화해도 LC이고, 제약값은 ε_M → 0만큼 변한다. 따라서 R_{p−ε,q} 쪽으로의 연속성(p에 대한 우연속)이 필요하며 이는 따로 적어야 한다. **남은 일: 절단 극한과 Dirac 경계(열린/닫힌 구간)의 정리.**

**명제 2(닫힌 식).** 밀도 ∝ e^{βw} on [a, b]에 대해

  ∫_a^b e^{βw} Φ((c−w)/√x) dw = β⁻¹[ e^{βb}Φ((c−b)/√x) − e^{βa}Φ((c−a)/√x) + e^{βc+β²x/2}{Φ((b−c−βx)/√x) − Φ((a−c−βx)/√x)} ]

(부분적분). 따라서 noisy 질량과 |W|의 분위수는 Φ만으로 계산된다. 구현 `src/uai/extremal.py`는 로그 공간에서 계산하며 적응 적분과 9×10⁻¹² 이내로 일치한다.

**명제 3(2차원 축소).** 모양 (β, ℓ = b − a)을 고정하고 이동 c만 바꾸면, c ↦ P(|W + c + e| ≤ 1)과 c ↦ P(|W + c| ≤ s)는 모두 LC 함수의 합성곱이라 log-concave다(Prékopa). 따라서 가능한 c는 구간을 이루고 Q_q(|W + c|)는 c에 대해 준볼록이므로, 최댓값은 구간의 두 끝점 중 하나에서 나온다. 그래서 R은 (β, ℓ) 2차원에서의 최대이며, 각 점의 값은 근 찾기 두 번과 닫힌 식으로 얻는다. 최적화기가 필요 없고, 격자와 Lipschitz 여유로 인증된 상한을 만들 수 있다(**남은 일**).

## 2. 작은 잡음 법칙 — 하한 **증명**, 상한 **증명 골격**(빈틈 2개), 명시식 **수치**

**정리 4.** 모든 q ∈ (0, 1)에 대해 R_{q,q}(x) ≥ 1 + c_q √x + o(√x), 여기서

  c_q = sup_{u>0} (1/u)·ln[ e^{u²/2} Φ(b_u − u) + e^{u b_u} Φ(−b_u) ] > 0

이고 b_u는 Φ(−b) + e^{−ub+u²/2}Φ(b − u) = q의 해다.

증명. 척도화한 한쪽 문제를 본다. Y = b − E, E ~ Exp(u)이면 P(Y + Z ≤ 0) = Φ(−b) + e^{−ub+u²/2}Φ(b−u)이고, 이 값은 b에 대해 엄격히 감소한다(도함수 −u e^{−ub+u²/2}Φ(b−u)). b = b_u로 두면 Y의 q-분위수는 b − ln(1/q)/u이고, 제약식을 대입하면 위 로그 표현이 된다. u → 0이면 b_u ≈ ln(1/q)/u → ∞이므로 Φ(u − b_u)는 u²/2보다 빨리 0으로 가서 로그 안이 1보다 크다. 그래서 작은 u에서 값 ≈ u/2 > 0이고 c_q > 0이다. 원래 문제로는 W = 1 + √x Y를 [−1 + δ, ∞)에서 자르면 된다. 반대쪽 끝 −1은 √x 척도로 거리 2/√x에 있으므로 영향은 o(√x)다(엄밀화할 부분: 절단 오차가 지수적으로 작음).

수치 값: c_.80 = 0.0461, c_.85 = 0.0316, c_.90 = 0.0191, c_.95 = 0.0084, c_.99 = 0.0014.

**정리 5(등호, 상한 쪽 — 증명 골격, 기술적 빈틈 두 개).** 척도화한 한쪽 문제

  c_{p,q} = sup{ F_Y⁻¹(q) : Y ∈ LC, P(Y + Z ≤ 0) ≥ p },  c_q = c_{q,q}

에 대해 lim_{x→0} (R_{q,q}(x) − 1)/√x = c_q다. 정리 4의 지수꼬리 식은 c_q의 하한이다. 두 값이 같다는 것(보조정리 A)은 **수치**다: 극값족 전체 격자에서 차이 ≤ 3×10⁻⁸.

상한 증명.
1. **대칭.** W ↦ −W에서 두 양이 모두 불변이므로 β ≥ 0(밀도 증가)인 log-affine W만 보면 된다(명제 1). Dirac은 noisy 질량 ≥ q이면 |d| < 1이라 값이 1 미만이다.
2. **왼쪽 끝은 손해만 본다.** β ≥ 0이면 F는 (−∞, b]에서 볼록이다. noisy − latent = {E F(1−e) − F(1)} − {E F(−1−e) − F(−1)}이고, Jensen으로 둘째 괄호는 ≥ −τ다. τ는 −1−e > b일 때의 보정으로, b > 1이면 P(e < −2)·(밀도 상한)이라 지수적으로 작다. 그러므로 noisy 질량 ≤ E F(1−e) − F(−1) + τ.
3. **−1 아래를 잘라낸다.** m = F(−1)로 두고 W̃ = W | W ≥ −1(여전히 LC), F̃ = (F − m)/(1 − m)으로 둔다. F − m ≤ (1 − m)F̃이므로 E F̃(1−e) ≥ (q − τ)/(1 − m) = q′ − τ′다. 여기서 q′ = q/(1 − m) ≥ q, τ′ = τ/(1 − m) ≤ τ/q다.
4. **분위수 비교.** F(s₀) − F(−1) = q, 즉 F̃(s₀) = q′인 s₀ ≥ 1에서 F(s₀) − F(−s₀) ≥ q이므로 Q_q(|W|) ≤ s₀다.
5. **척도.** Y = (W̃ − 1)/√x로 두면 P(Y + Z ≤ 0) = E F̃(1 − √x Z) ≥ q′ − τ′이고 F_Y⁻¹(q′) = (s₀ − 1)/√x다. 따라서 (s₀ − 1)/√x ≤ c_{q′−τ′, q′}다(W̃ ≥ −1이라는 지지 제한을 버리면 sup은 커지기만 한다).
6. **보조정리 B(대각선 단조성, 증명).** q′ > q이고 Y가 (q′, q′)에서 가능하면, 왼쪽 질량 m_L = (q′ − q)/(1 − q)을 잘라낸 Y* = Y | Y ≥ L은 P(Y* + Z ≤ 0) ≥ (q′ − m_L)/(1 − m_L) = q이고 F_{Y*}(v) = q다(v = F_Y⁻¹(q′)). 그래서 c_{q,q} ≥ c_{q′,q′}다. 같은 절단을 제약에 여유 τ′를 두고 적용하면 c_{q′−τ′, q′} ≤ c_{q−τ″, q}, τ″ = τ′/(1 − m_L)이다.
7. 결론: R_{q,q}(x) ≤ 1 + √x · c_{q−τ″, q}. τ″ → 0이면 c_{p,q}의 p에 대한 연속성으로 상한 1 + c_q√x + o(√x)을 얻는다.

**남은 빈틈.**
- (i) τ의 균일한 지수적 작음: β가 매우 크면(e^{1/x} 수준) 밀도 상한이 커진다. 그 경우 W는 한 점 근처에 몰리므로 Dirac 근사로 따로 처리한다.
- (ii) c_{p,q}의 p에 대한 (대각선 근처) 연속성. τ″는 1 − m_L이 작을 때(q′ → 1) 커질 수 있으므로 q′ → 1 쪽을 따로 다뤄야 한다. c_{q′,q′} → 0(q′ → 1)이라 그 영역은 상한에 영향이 없을 것으로 보인다.
- 하한(정리 4)은 한쪽 문제의 거의 최적인 Y를 −2/√x에서 잘라 옮기며, 오차는 지수적으로 작다.

수치 확인(이전의 추측 5 근거):
- (0) 명제 3의 정확한 2차원 계산(E16)이 x = 10⁻⁶, 10⁻⁵, 10⁻⁴에서 R = 1.0000190, 1.0000604, 1.0001906을 주고, 1 + c_.9√x = 1.0000191, 1.0000603, 1.0001906과 일치한다.
- (i) 척도화한 한쪽 문제의 극값족 전체(β의 부호 포함, 길이 10⁻³–2×10⁴) 격자에서 최댓값은 지수꼬리 값에 아래에서 수렴한다(q = .95, .99에서 차이 3×10⁻⁸).

**해석.**
- 대칭이면 잡음은 항상 보수적이다(Anderson). 비대칭 log-concave에서는 **모든 수준 q에서** 잡음이 작을 때 noisy 보정이 잠재 목표를 √x 차수로 놓칠 수 있다.
- 기구: 끝점 1 바로 바깥에서 밀도가 지수적으로 올라가다 잘리는 분포(경계층 폭 √x)에서, 잡음이 바깥 질량을 안으로 끌어들이는 양이 안쪽 질량을 밖으로 내보내는 양보다 크다.
- 테일러 전개로 기대되는 차수 O(x)(f′(1) − f′(−1) 항)보다 느린 √x인 것은 그 경계층 때문이다.
- 이전 검토에서 “작은 x의 r > 1은 구적 오차”라고 한 것은 크기(1.027)에 대해서만 맞았다. 참값은 x = 10⁻³에서 약 1.0005–1.0006이다.

**따름정리 6(PAC 여유).** 작은 잡음에서 p > q이면 척도화 상수 c_{p,q}(같은 지수꼬리 식에서 ln(1/q)만 유지하고 제약을 p로)는 p − q가 충분히 크면 음수가 된다. 필요한 최소 여유(지수꼬리족 기준)는 q = .80, .90, .95에서 각각 4.4×10⁻³, 8.0×10⁻⁴, 1.6×10⁻⁴이다. K = 110, k = 105의 Beta 여유 p_k − q = .0036은 q = .90에서 이를 흡수하므로, PAC 절차에서는 작은 잡음 확대가 필요 없다(**수치**; 정리 5와 보조정리 A에 의존).

## 3. 축소·유지·확대의 경계 — **수치**

p = q에서 R_{q,q}(x) = 1이 되는 x*(q)와 최대 확대(E19, `results/boundary_map.csv`):

| q | x*(q) | sup_x R_{q,q}(x) | 최대점 x |
|---|---|---|---|
| .80 | .0344 | 1.00370 | .0128 |
| .85 | .0167 | 1.00174 | .0054 |
| .90 | .0062 | 1.00063 | .0023 |
| .95 | .0012 | 1.00012 | .0004 |

x < x*(q)이면 noisy 임계값을 **넓혀야** 하고(잡음이 오히려 포함률을 부풀릴 수 있다), x > x*(q)이면 줄일 수 있다. 대칭이면 모든 x에서 줄일 수 있거나 최소한 유지할 수 있다(Anderson). 확대량은 작다(최대 0.4%). 이론적 의미는 “잡음 보정이 보수적”이라는 통념의 경계를 정확히 긋는 데 있다.

DE 표(E04)와 정확한 2차원 계산(E16)은 x ≥ .006의 모든 표 점에서 10⁻¹³ 이내로 일치한다. 서로 독립인 두 방법이다. x = .001의 DE 값 1.027은 구적 오차였고 참값은 1.000547이다.

## 4. 이질 잡음 — 절차는 **증명**, 크기는 **수치**

지역 i의 잡음이 N(0, D_i)이고 D_i가 알려져 있다(FH 관행). V_i는 독립이지만 동일분포가 아니다.

**명제 7(순서통계량의 Beta 하한은 이질 잡음에서도 유효).** F̄를 |V_i|의 평균 CDF라 하고 T = |V|_(k), t_p = F̄⁻¹(p)라 하자. {F̄(T) < p} = {#{|V_i| ≤ t_p} ≥ k}이고, 이 개수는 평균 Kp인 Poisson-binomial이다. Hoeffding(1956)에 따라 c ≥ Kp + 1이면 P(PoisBin ≥ c) ≤ P(Bin(K, p) ≥ c)이므로, p_k = Beta(k, K+1−k)의 δ 분위수에 대해 P(F̄(T) ≥ p_k) ≥ 1 − δ다(k ≥ Kp_k + 1일 때; K = 110, k = 105, p_k = .9036에서 성립).

**따름정리 8(HetLDC).** 그 사건 위에서 ∫ ḡ_T dG ≥ p_k이고, ḡ_T는 x_i = D_i/T²에서의 가우스 커널 평균이다. 이것도 선형 제약 하나이므로 명제 1의 극값족이 그대로이고, 반폭 s = T·R^{mix}_{p_k,q}(x_1, …, x_K)는 모든 LC G에서 잠재 포함률 ≥ q를 준다. 계산량을 줄이려고 x_i를 32개 묶음 평균으로 양자화하고, 커널 차이의 상한 ε(격자 최대 + Mh²/8)을 p_k에서 뺀다(`procedures.quantised_kernel`, 보통 ε ≈ 2×10⁻⁴). 혼합 커널은 log-concave가 아니어서 가능한 이동이 여러 구간일 수 있지만, 잠재 분위수는 이동에 대해 준볼록이므로 가장 바깥 두 끝점만 보면 된다.

**E18(평균 분산 대입의 오류 방향).** x̄와 로그정규 퍼짐 σ에서 정확한 혼합 커널의 R과 평균 분산 대입 R(x̄)를 비교하면:
- 대부분 혼합 쪽이 더 작다(대입이 보수적, 최대 .008 차이).
- x̄ = .2, σ = 1.0에서는 혼합 R = .7648 > 대입 .7542다. **평균 분산 대입은 1.4% 짧아 보장을 잃는다.**
- 반대로 “안전하게” 가장 작은 분산을 대입하면 최대 14% 넓어진다(x̄ = .136, σ = .7: .986 대 .863).

## 5. 다음

1. 명제 3으로 R_{p,q}의 인증된 상한 표를 만든다: (β, log ℓ) 격자 셀마다 끝점 값의 상한, 그리고 격자 밖(β → ∞, ℓ → 0, ℓ → ∞) 극한의 해석적 처리.
2. 정리 5의 빈틈 (i), (ii)를 메우고, 보조정리 A(한쪽 문제의 극값이 지수꼬리)를 증명한다.
3. HetLDC의 합성 평가(E20)와 apipop 적용.
4. 짧은 원고의 핵심 3쪽 초안.
